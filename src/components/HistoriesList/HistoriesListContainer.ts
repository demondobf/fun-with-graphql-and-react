import styled from 'styled-components/macro';

const HistoriesListContainer = styled.section`
  display: grid;
  gap: 1em;

  max-width: 80ch;
  margin: 0 auto;
`;

export default HistoriesListContainer;
